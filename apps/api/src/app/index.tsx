import { Text, View } from "react-native";

// The invitation acceptance landing page arrives with the identity task.
export default function LandingPage() {
  return (
    <View>
      <Text>Handoff API</Text>
    </View>
  );
}
